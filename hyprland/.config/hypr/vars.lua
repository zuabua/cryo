-- Shared values, required by both hyprland.lua and binds.lua.
-- The Lua equivalent of hyprlang's `$mainMod = SUPER` variables.

return {
    mainMod     = "SUPER",
    terminal    = "kitty",
    fileManager = "thunar",
    menu        = "hyprlauncher",
}
